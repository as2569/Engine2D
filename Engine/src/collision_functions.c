//#include "entity.h"
//#include "collision_functions.h"

//int point_in_entity(int x, int y, Entity* e)
//{
//	if (!e)
//	{
//		slog("Not a valid entity");
//		return 0;
//	}
//
//	if ((x >= e->bounding_box.x)
//		&& (x <= (e->bounding_box.x + e->bounding_box.w))
//		&& (y >= e->bounding_box.y)
//		&& (y <= (e->bounding_box.y + e->bounding_box.h)))
//		return 1;
//};